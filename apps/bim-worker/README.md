# apps/bim-worker — NOT YET IMPLEMENTED

**Gate:** Sprint ~33 (IFC / BIM interoperability).

Python worker for server-side IFC parsing and export via
IfcOpenShell. Created only when BIM import/export has a real
consumer (ADR-003, ADR-012, ADR-018).

Do not add build, CI, or runtime configuration before that gate.
