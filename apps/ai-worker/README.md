# apps/ai-worker — NOT YET IMPLEMENTED

**Gate:** Sprint ~31 (Upload Plan / drawing recognition pipeline).

Python / FastAPI worker for computer vision and ML inference
(OpenCV, PyTorch, ONNX). Created only when the recognition
pipeline has a real consumer (ADR-003, ADR-012).

Do not add build, CI, or runtime configuration before that gate.
